# Runtime code (src/) uses the Python standard library only: deterministic agent,
# JSON knowledge base, and file-backed session memory.
#
# Install dev/test tooling with: pip install -r requirements-dev.txt
# Optional LangChain/LangGraph pins (unused by current code paths): requirements-llm.txt

pytest==8.3.3
